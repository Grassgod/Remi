/**
 * `remi login` — Interactive setup wizard.
 * TODO: Implement in Phase 2.
 */

export async function runLogin(_args: string[]): Promise<void> {
  console.log("remi login — not yet implemented. Coming in Phase 2.");
  console.log("For now, manually configure ~/.remi/remi.toml and run `remi auth`.");
}
